# Client_Query_Management-System
A Client Query Management System built using Python, Streamlit, and MySQL. Clients can log in to submit queries and track their status, while support users can view all incoming queries, monitor open vs closed tickets, and close queries directly from a dashboard.
